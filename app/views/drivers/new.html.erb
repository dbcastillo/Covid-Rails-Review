<%= form_for @driver do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :customer %>
  <%= f.collection_select :customer_ids, Customer.all, :id, :name %>

  <%= f.submit %>
<% end %>